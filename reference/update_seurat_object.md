# Update Seurat Object if Necessary

Checks if the Seurat object is from an old version (v2) and updates it
to the latest format.

## Usage

``` r
update_seurat_object(data)
```

## Arguments

- data:

  A Seurat object.

## Value

An updated Seurat object.
