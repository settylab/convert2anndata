% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/anndata_names.R
\name{anndata_names}
\alias{anndata_names}
\title{Extract obs/var names from an AnnData object}
\usage{
anndata_names(adata)
}
\arguments{
\item{adata}{An AnnData object (e.g. as returned by `anndata::read_h5ad`).}
}
\value{
A list with elements `obs_names` (cells) and `var_names` (features),
  each a character vector or `NULL` if extraction failed.
}
\description{
Cell and feature names on AnnData objects are Python pandas Indexes;
R's `rownames()`/`colnames()` do not return them reliably. This helper
coerces them to character vectors via Python's `list()` builtin, falling
back to `names()` if that path fails.
}
